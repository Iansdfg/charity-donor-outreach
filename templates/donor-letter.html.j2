<!doctype html>
<html lang="en">
<head><meta charset="utf-8"><title>{{ narrative.subject }}</title></head>
<body>
<main style="font-family: Georgia, serif; max-width: 40rem; color: #222">
  <p>{{ bundle.salutation }}</p>
  <p>{{ narrative.opening }}</p>
  <p>{{ narrative.campaign_paragraph }}</p>
  <p>{{ narrative.ask_paragraph }}</p>
  <p><a href="{{ donation_url }}">Donate securely</a>.</p>
  <p>With gratitude,<br>{{ charity_name }}</p>
</main>
</body>
</html>

